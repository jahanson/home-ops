---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mastodon-system
  namespace: fediverse
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-block
